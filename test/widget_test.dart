import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App khởi tạo thành công', (WidgetTester tester) async {
    // TODO: Thêm test khi implement features
    expect(1 + 1, equals(2));
  });
}
